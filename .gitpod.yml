tasks:
- init: sudo apt update -y && sudo apt upgrade -y && python -m pip install --upgrade pip && pip install virtualenv && virtualenv venv && source venv/bin/activate && pip install -r requirements.txt && python __init__.py
vscode:
  extensions:
    - ms-python.python
    - eamodio.gitlens